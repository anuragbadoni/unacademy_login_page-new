

const emailInput = (props) => {
    return (
        <div className="">
            <input
                className="w-full h-16 border border-radius rounded-md"
                placeholder="Email address"
                type="email"
                value=""
            />
        </div>
    );
};

export default emailInput;
